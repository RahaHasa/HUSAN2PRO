# 🚀 Деплой RENT MEYRAM на Railway

## Шаг 1: Регистрация на Railway

1. Откройте https://railway.app
2. Нажмите **"Start a New Project"**
3. Войдите через GitHub

## Шаг 2: Создание PostgreSQL базы данных

1. В Railway нажмите **"+ New"**
2. Выберите **"Database" → "PostgreSQL"**
3. Дождитесь создания
4. Скопируйте переменные подключения (DATABASE_URL)

## Шаг 3: Деплой Backend

1. Нажмите **"+ New" → "GitHub Repo"**
2. Выберите репозиторий проекта
3. Выберите папку **"backend"**
4. Добавьте переменные окружения:
   ```
   DATABASE_URL=<скопируйте из PostgreSQL сервиса>
   JWT_SECRET=cinema_rental_secret_key_2026_production
   PORT=3000
   EMAIL_HOST=smtp.gmail.com
   EMAIL_PORT=587
   EMAIL_USER=rohahoha87@gmail.com
   EMAIL_PASSWORD=ojcdcpfiecqaydvb
   NODE_ENV=production
   ```
5. Railway автоматически задеплоит backend
6. Скопируйте URL backend (например: `https://backend-production-xxxx.up.railway.app`)

## Шаг 4: Деплой Frontend

1. Нажмите **"+ New" → "GitHub Repo"**
2. Выберите папку **"frontend"**
3. Добавьте переменные окружения:
   ```
   NEXT_PUBLIC_API_URL=<URL вашего backend из шага 3>
   NODE_ENV=production
   ```
4. Railway автоматически задеплоит frontend
5. Ваш сайт будет доступен по URL!

## Шаг 5: Обновление API URL в коде

После деплоя backend, обновите `lib/api-new.ts` в frontend с production URL.

## Готово! 🎉

Ваш проект RENT MEYRAM теперь доступен онлайн!

## Автоматические обновления

Каждый push в GitHub будет автоматически деплоиться на Railway.

## Полезные команды

```bash
# Установка Railway CLI
npm install -g @railway/cli

# Логин
railway login

# Просмотр логов
railway logs

# Открыть проект в браузере
railway open
```

## Мониторинг

- Логи: Railway Dashboard → Your Service → Logs
- Метрики: Railway Dashboard → Your Service → Metrics
- База данных: Railway Dashboard → PostgreSQL → Connect

## Стоимость

- **$5/месяц** - включает все сервисы
- Первый месяц может быть бесплатным (trial)
